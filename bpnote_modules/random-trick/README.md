# Random Trick

Module simple qui choisit un trick au hasard parmi les tricks visibles dans BypassNote.

## Installation

Ce module peut être installé en sélectionnant le dossier ou en le packant en .zip avec les fichiers suivants :
- manifest.json
- index.js
